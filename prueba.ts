function suma(a: number, b: number) : number {
    return a + b
}



let myArray : Array<number> = [1,2,3];

function agregarArray (arr: Array<number>, 
    element: number) : void {
    arr.push(element);

}


//tipos